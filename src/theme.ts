import {createTheme} from '@mui/material';

const theme = createTheme({
	palette: {
		primary: {
			main: '#ec623d',
		},
		secondary: {
			main: '#555555',
		},
		background: {
			default: '#eeeeee',
		},
	},
});

export default theme;
